# gestionClientes

Esta aplicación fue desarrollada en Java y permite gestionar registros de clientes mediante operaciones CRUD (crear, leer, actualizar y eliminar).
Está construida utilizando JPA (Jakarta Persistence API) con Hibernate ORM como proveedor de persistencia, y almacena los datos en una base de datos MySQL llamada clientes.

El objetivo principal es demostrar el uso de:

Validación de datos de entrada mediante estructuras de control (condicionales if, bucles, etc.).

Uso de colecciones (List, ArrayList) para manejar datos en memoria antes de persistirlos en la base de datos.

Conexión y persistencia con JPA/Hibernate.
# Gestion-Clientes
